{
  "name": "@nentang/tls-authorize",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Endpoint `ask` cho Caddy on-demand TLS. Không framework, một phụ thuộc.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
